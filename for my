//function createBoard() { لانشاء البلاطات يعني شبكه 
//function generate() {//هذه الدالة مسؤولة عن إضافة رقم 2 في مربع فارغ عشوائي في الشبكة
//function getMaxTile() {//تأخذ كل الأرقام وتحسب أكبر رقم بينهم.
//function move(direction) {//الدالة مسؤولة عن تسجيل حركة اللاعب، حفظ بيانات تحليل اللعبة، وتعطي توصيات ذكية بسيطة بناءً على طريقة اللعب.
//gameAnalyticsLog.push({  // حفظ بيانات الحركة في سجل تحليلات اللعبة
//function showRecommendation(message) {عرض رسالة توصية داخل عنصر HTML معين لفترة محددة ثم إخفائها تلقائيًا.
////function saveLatestMove تجمع بيانات ,ترسل هذه البيانات إلى خادم (Model API) ,تعرض مؤشر خطر أو تحذير إذا كان التنبؤ يشير إلى وجود مخاطرة
//function drawDangerGauge رسم مؤشر دائري (Gauge) يوضح مستوى الخطر باستخدام مكتبة Chart.js.
//function combineدمج الأرقام المتشابهة  للصفوف وتحت للاعمده 
// function control(e) {كود يتحكم بحركات اللاعب بناءً على ضغطات مفاتيح الأسهم
//function checkForWin() {للفوز 
//winCount++; // زيادة عدد مرات الفوز
//function checkForGameOver() { للخساره
// lossCount++;  زيادة عدد مرات الخساره
//function showStats() {تعرض إحصائيات اللعبة مثل عدد الحركات، وتحفظ بيانات التحليل، وترسم رسوم بيانية توضح أداء اللعبة.
 //function generateCSVAnalytics() {// دالة تنشئ ملف CSV يحتوي على تحليلات اللعبة
//generateCSVAnalytics() تمر على كل حركة في gameAnalyticsLog، وتضيفها كسطر مستقل في الملف بصيغة CSV، 
//function drawCharts() { لوحة تحكم تحليلات اللاعب 🎮في الجافاسكربت
//<section class="dashboard">لوحة تحكم تحليلات اللاعب 🎮 في الانديكس 
//-----------------------------------------------------------------------------------------------------------------------
//أسماء الفانكشين المسؤولة عن الداتا (البيانات) في اللعبة:
//generateCSVAnalytics()

//توليد ملف CSV يحتوي على سجل كامل للحركات مع بياناتها (الاتجاه، النقاط، الوقت، إلخ).

//drawCharts()

//رسم الرسوم البيانية (Charts) لتحليل بيانات اللعبة مثل عدد الحركات، عدد الفوز والخسارة، أوقات الحركات، وأعلى بلاطة.

//showStats()

//عرض إحصائيات اللعبة (يستدعي generateCSVAnalytics() و drawCharts()).

//checkForWin()

//التحقق من الفوز (الوصول إلى 2048) وتحديث البيانات المرتبطة.

//checkForGameOver()

//التحقق من انتهاء اللعبة (لا توجد بلاطات فارغة) وتحديث البيانات المرتبطة.

//control(e) (جزء منها)

//يتعامل مع مدخلات لوحة المفاتيح ويُشغل تحركات اللعبة التي تؤثر على البيانات.

//keyLeft() / keyRight() / keyUp() / keyDown()

//تنفيذ الحركات مع دمج البلاطات، توليد بلاطات جديدة، وتحديث سجل البيانات.

